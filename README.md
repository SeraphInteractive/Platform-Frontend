# @platform/vote-ui

React and TanStack Query frontend for the Minecraft movie ranked voting platform. Connects directly to vote-api and uses @platform/internal-logic for statistical verification.

## Setup

```bash
npm install
npm run dev
```

App runs on `http://localhost:5173`.
